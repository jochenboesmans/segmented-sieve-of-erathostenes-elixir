defmodule SegmentedSieveOfErathostenesTest do
  use ExUnit.Case
  doctest SegmentedSieveOfErathostenes

  test "greets the world" do
    assert SegmentedSieveOfErathostenes.hello() == :world
  end
end
